import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AstrologerType = () => {
  return (
    <View>
      <Text>AstrologerType</Text>
    </View>
  );
};

export default AstrologerType;

const styles = StyleSheet.create({});
